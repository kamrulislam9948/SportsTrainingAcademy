import { Applicant } from "./applicants";

export class SelectedPlayer {
    selectedPlayerId?: number ;
    applicantId?: number ;
    applicant?: Applicant;
    eventId?: number;
    event?: Event;
  }